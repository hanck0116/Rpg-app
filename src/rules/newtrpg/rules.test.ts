import {describe,it,expect} from 'vitest';import{parseDiceExpression,rollDiceExpression}from'./dice';import{parseEffectString}from'./items';import{createCharacter}from'./schema';import{calculateEffectiveStats,calculateMaxHp,calculateAttackStats,calculateFinalDifficulty}from'./calculation';import{performCheck,performOpposedCheck}from'./judgement';import{applyExperience,calculateAdventureRank}from'./experience';
describe('newtrpg rules',()=>{it('parses complex dice',()=>{expect(parseDiceExpression('2d6+1d4+3')).toHaveLength(3);const r=rollDiceExpression('1d100+10-1d6',()=>0);expect(r.total).toBe(105)});it('parses effects',()=>{const b=parseEffectString('힘 +1; 물리공격력 +2\n최대HP +10, 난이도 -10');expect(b.stats?.힘).toBe(1);expect(b.physicalAttack).toBe(2);expect(b.maxHp).toBe(10);expect(b.difficulty).toBe(-10)});it('calculates stats hp attacks difficulty',()=>{const c=createCharacter();c.equipment=[{id:'e',name:'검',kind:'장비',effect:'힘 +1, 물리공격력 +2',bonus:parseEffectString('힘 +1, 물리공격력 +2')}];expect(calculateEffectiveStats(c).힘.final).toBe(11);expect(calculateMaxHp(c)).toBe(100);expect(calculateAttackStats(c).physicalAttack).toBe(12);expect(calculateFinalDifficulty(c,60)).toBe(55)});it('judges checks',()=>{const c=createCharacter();expect(performCheck(c,'힘',20,()=>0).success).toBe(true);expect(performOpposedCheck(c,'힘',c,'힘',()=>0).result).toBe('재판정 필요')});it('levels and ranks',()=>{expect(applyExperience(1,0,0,100).level).toBe(2);expect(calculateAdventureRank(90)).toBe('EX')});});
